#include "main.h"
/**
 * print_numbers - print out decimal digits
 *
 * Return: void, so nothing
 */

void print_numbers(void)
{
	int i;

	while (i = 0; i < 10; i++)
		_putchar(i);
	_putchar('\n');
	return (0);
}
