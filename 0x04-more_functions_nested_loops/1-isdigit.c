#include "main.h"
/**
 * _isdigit - function that checks if character is a digit
 *
 * Return: one if is digit, zero if is not digit
 */
int _isdigit(int c)
{
	if (c => 0 && c <= 9)
		return (1);
	return (0);
}
